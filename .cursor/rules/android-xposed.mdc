---
description: Android Xposed 模块与 DataBinding 开发约定
globs:
  - "**/*.kt"
  - "**/*.xml"
alwaysApply: false
---

# Android Xposed 项目约定

## 语言

- 仅 Kotlin，路径 `src/main/kotlin/`、`src/test/kotlin/`
- 禁止新建 `src/**/java/` 目录或 `.java` 源文件

## UI

- DataBinding + ViewModel，禁止 Jetpack Compose
- Activity 用 `DataBindingUtil.setContentView`
- 列表用 RecyclerView + ListAdapter

## Xposed

- Hook 放 `xposed/`，入口写入 `assets/xposed_init`
- Xposed API 仅 `compileOnly`，宿主侧反射检测框架
- `XposedBridge.log` 打日志，精准捕获异常
- **通用 Hook 原则**（强制）：见 `.cursor/rules/hook-scope.mdc` — 系统层四通道，禁止针对单一 App / `libdevice.so` 等特化；`com.android.device` 仅作验证参考

## AI 工具

- 任务前：Superpowers + Caveman（见 `ai-workflow.mdc`）
- 改代码前：Codegraph 查调用链
- 大改动后：`/understand` 更新图谱
